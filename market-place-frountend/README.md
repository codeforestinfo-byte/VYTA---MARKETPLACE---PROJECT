# VYTA - Fitness Marketplace

Multi-vendor gym & fitness marketplace built with React + Vite.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
